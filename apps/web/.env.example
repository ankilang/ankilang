# Configuration Appwrite pour Ankilang
# =====================================
# 
# Instructions :
# 1. Copiez ce fichier vers .env.local
# 2. Remplacez les valeurs factices par vos vraies valeurs Appwrite
# 3. Ne commitez JAMAIS le fichier .env.local (déjà dans .gitignore)

# Endpoint de votre instance Appwrite
# Votre instance Frankfurt d'Appwrite Cloud
VITE_APPWRITE_ENDPOINT=https://fra.cloud.appwrite.io/v1

# Project ID de votre projet Appwrite
# Projet configuré : "ankilang"
VITE_APPWRITE_PROJECT_ID=ankilang

# ⚠️ IMPORTANT : 
# Ces variables commencent par VITE_ car elles sont utilisées côté client.
# Elles seront donc exposées publiquement dans le bundle JavaScript.
# Ne mettez jamais de secrets (API Keys, Master Keys) dans ces variables !
#
# Les clés secrètes (API Key, Master Key) sont configurées dans :
# - Netlify Functions (variables d'environnement Netlify)
# - Variables d'environnement serveur pour l'exporter FastAPI